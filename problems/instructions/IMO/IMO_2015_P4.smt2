(param (A B C) triangle)
(compute Omega circle (circumcircle A B C))
(compute O point (circumcenter A B C))

;; Parameterize a circle by (a) origin or (b) radius
;; Parameterize a line by a point it passes through
;; Enforce order on circle by oppSides
